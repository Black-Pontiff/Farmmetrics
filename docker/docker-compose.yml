services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports: ["8000:8000"]
    env_file: [../.env]
    volumes: [farm-data:/data, ../dashboard:/app/dashboard:ro]
    restart: unless-stopped
volumes:
  farm-data:
